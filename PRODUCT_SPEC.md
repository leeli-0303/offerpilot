# OfferPilot Product Specification

## 1. 产品定位

OfferPilot 是一款面向应届生的 AI 求职管理助手，帮助用户统一管理岗位信息、简历版本、投递进度和面试记录，
并通过 AI 自动完成 JD 解析、岗位-简历匹配、下一步行动建议和求职周报生成。

## 2. 目标用户

- **主要用户**：正在参加秋招 / 春招的应届毕业生（本科、硕士）。
- **次要用户**：有实习需求的在校生、毕业 1-2 年内仍在求职的往届生。
- **用户特征**：同时投递多家公司、多个岗位，需要维护多版简历，容易遗漏投递节点和面试安排。

## 3. 核心痛点

| 痛点 | 说明 |
|------|------|
| 信息分散 | 岗位信息散落在招聘官网、邮箱、微信、飞书等多个渠道，缺乏统一管理。 |
| 简历版本混乱 | 针对不同岗位需要调整简历，中英文、不同岗位方向的版本难以维护。 |
| 进度追踪困难 | 投递后容易忘记进度，面试轮次、反馈结果、下一步行动缺乏汇总。 |
| 决策成本高 | 面对大量 JD，难以快速判断岗位与自身匹配度，不知优先投递哪个。 |
| 复盘无据 | 求职周期结束后无法系统复盘，不清楚哪个环节需要改进。 |

## 4. MVP 功能范围

### 4.1 岗位管理
- 手动创建 / 编辑 / 删除岗位。
- 粘贴 JD 文本自动解析（AI），提取公司、岗位名称、薪资范围、地点、岗位要求、加分项。
- 岗位列表，支持按状态（有意向 / 已投递 / 面试中 / 已 Offer / 已拒绝）筛选和排序。
- 岗位标签（如：大厂、外企、急招、内推）。

### 4.2 简历管理
- 上传简历文件（PDF / Word / 纯文本），系统解析为结构化文本。
- 支持多版简历（如：后端-中文、后端-英文、前端-中文），标记版本说明。
- 简历字段摘要展示（教育背景、技能标签、实习经历等）。

### 4.3 投递进度追踪
- 为每个岗位记录投递日期、投递渠道（官网 / BOSS / 内推 / 校招）、简历版本、备注。
- 进度看板（卡片式 / 列表式），可视化展示各岗位当前阶段。
- 面试记录：记录面试时间、轮次、面试官反馈、自评。

### 4.4 AI 核心能力
- **JD 解析**：粘贴 JD 文本 → 自动提取结构化字段。
- **岗位-简历匹配**：给定岗位和简历版本 → 输出匹配度评分 + 差距分析 + 简历优化建议。
- **下一步行动建议**：根据各岗位当前进度 → 提醒即将到期的面试、建议优先准备或跟进。
- **求职周报**：汇总本周新增岗位、投递数量、面试进展、下周关注点。

### 4.5 数据看板（Dashboard）
- 关键指标卡片：总投递数、面试中数、Offer 数、转化率。
- 近期时间线：最近一周的投递 / 面试事件。
- AI 行动建议摘要。

## 5. 非 MVP 范围

- 自动抓取招聘网站 JD（MVP 阶段手动粘贴或文本录入）。
- 多用户 / 团队协作。
- 移动端 App（MVP 为 Web 响应式即可）。
- 邮件 / 日历自动同步。
- 企业内部招聘流程对接（ATS 集成）。
- 语音面试模拟。
- 社交分享与匿名爆料（如 Offer 薪资比较社区）。

## 6. 页面结构

```
├── Dashboard          # 数据看板（总览）
├── Jobs               # 岗位列表
│   └── Job Detail     # 岗位详情（含 JD 解析结果、匹配分析）
├── Resumes            # 简历列表
│   └── Resume Detail  # 简历详情（解析结果、关联岗位）
├── Tracker            # 投递进度看板
│   └── Interview Log  # 面试记录详情
├── Reports            # 求职周报
└── Settings           # 个人设置
```

## 7. 核心数据对象

### Job（岗位）
```
id, company, title, status, jd_raw_text, jd_parsed_fields(JSON),
location, salary_range, tags, source_url, notes, created_at, updated_at
```

### Resume（简历）
```
id, version_name, file_path, parsed_content(JSON), summary_fields(JSON),
tags, created_at, updated_at
```

### Application（投递记录）
```
id, job_id, resume_id, applied_date, channel, current_stage, status,
notes, created_at, updated_at
```

### Interview（面试记录）
```
id, application_id, round, interview_date, interviewer_feedback,
self_rating, notes, created_at, updated_at
```

### AIReport（AI 报告）
```
id, type(JD_PARSE / MATCH / WEEKLY), related_ids(JSON), content(JSON),
created_at
```

## 8. AI 工作流

| 工作流 | 触发方式 | 输入 | 输出 | 存储为 |
|--------|---------|------|------|--------|
| JD 解析 | 用户粘贴 JD 文本并点击解析 | 原始 JD 文本 | 结构化字段（公司、岗位、要求、薪资等） | AIReport(type=JD_PARSE) + Job.jd_parsed_fields |
| 岗位-简历匹配 | 用户在岗位详情页选择简历版本并点击分析 | 岗位要求 + 简历内容 | 匹配度评分（0-100）、匹配点列表、差距列表、简历修改建议 | AIReport(type=MATCH) |
| 下一步行动建议 | 进入 Dashboard 时自动加载 / 手动刷新 | 所有岗位当前状态 | 优先级排序的行动建议列表 | 实时计算（不持久化）或缓存于 Dashboard |
| 求职周报 | 每周一自动生成 / 手动生成 | 本周所有活动记录 | 结构化周报（投递统计、面试进展、下周关注） | AIReport(type=WEEKLY) |

### AI 调用原则
- 所有 AI 调用需要用户显式触发或确认，避免自动消耗 token。
- JD 解析和匹配分析结果可缓存，同一输入不重复调用。
- 周报生成合并同一周内的多条记录为单次调用。

## 9. 与在线表格的差异

| 维度 | 在线表格（Excel / 飞书表格） | OfferPilot |
|------|---------------------------|------------|
| JD 录入 | 手动逐字段填写 | 粘贴原文，AI 自动提取 |
| 简历关联 | 手动标注或用超链接 | 结构化关联，一键匹配分析 |
| 进度可视化 | 需手动建图表 | 自动看板 + Dashboard |
| 匹配分析 | 人工肉眼对比 | AI 评分 + 差距分析 + 修改建议 |
| 行动提醒 | 需自己记得去看 | AI 主动推荐下一步 |
| 周报 | 手动汇总 | AI 自动生成 |
| 协作 | 天然支持多人编辑 | MVP 仅单用户 |

## 10. MVP 成功标准

- [ ] 用户能在 5 分钟内完成首次岗位录入和 JD 解析。
- [ ] 岗位-简历匹配分析结果让用户觉得有参考价值（Beta 用户反馈好评率 ≥ 70%）。
- [ ] 单个用户管理 50+ 岗位时页面无明显卡顿。
- [ ] 核心流程（录入岗位 → 关联简历 → 查看匹配 → 记录投递 → 看板追踪）能跑通且无阻断性 Bug。
- [ ] 获得 10 位种子用户持续使用 2 周以上，并完成反馈问卷。
